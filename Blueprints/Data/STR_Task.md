# STR_Task

Blueprint structure defining a mailbox task entry.

- Fields:
  - `TaskID` (Name)
  - `Title` (Text)
  - `Description` (Text)
  - `DueHour` (Integer, 0–23)
- Stored in `/Blueprints/Data/STR_Task` and used by the task data table and runtime arrays.
